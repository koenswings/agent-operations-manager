# Atlas Memory — 2026-03-26

## Session Context (pre-compaction flush)

### What Was Accomplished Today (structural build-out session)

- Archived `agent-researcher` and `agent-quality-manager` repos on GitHub
- Created `agent-operations-manager` repo; Atlas bound to Telegram group `-5105695997`
- Removed stale identity files, `research/` folder from idea org root
- Created `idea/design/`, `idea/docs/`, `idea/proposals/` with READMEs
- Moved `virtual-company-design.md` to `idea/design/`; marked `Approved · Implemented`
- Removed 5 stale agents from openclaw.json; Atlas named; gateway restarted
- Added `/init` command to all 5 agents' AGENTS.md
- Updated quality review references in all agent AGENTS.md (→ Atlas)
- Updated README.md: Atlas replaces Veri/Compass, MC port corrected (4000→8000)
- Updated virtual-company-design.md: Veri/Compass → Atlas, `/init` section, CLAUDE.md section, Outputs policy (Session Documentation section)
- Created CLAUDE.md for all 5 agents (pointer list only, no prose)
- Deleted old CLAUDE.md + PROJECT.md from engine-dev (both redundant)
- Renamed `auto-review` concept to `cross-agent requests` throughout design doc and `check-new-tasks.sh`
- Defined cross-agent request types: `Review:`, `Question:`, `Opinion:`, `Feasibility:`
- Updated `check-new-tasks.sh`: Veri removed, agent IDs corrected to `lead-*` format, Atlas commented out pending board ID

### Open PRs (awaiting Koen merge as of 2026-03-26 morning)

- **idea PR #4** (`docs/outputs-policy`) — outputs policy design doc section
- **idea PR #5** (`docs/cross-agent-requests`) — cross-agent rename
- **agent-engine-dev PR #13** (`memory/updates`) — Outputs section in AGENTS.md
- **agent-console-dev PR #9** (`memory/updates`) — Outputs section in AGENTS.md
- **agent-site-dev PR #9** (`memory/updates`) — Outputs section in AGENTS.md
- **agent-programme-manager PR #14** (`memory/updates`) — Outputs section in AGENTS.md
- **agent-operations-manager PR #4** (`memory/updates`) — Outputs section + first output file + cross-agent output file
- **agent-engine-dev PR #9** (`test/pr1-disk-simulation`) — pre-existing Axle test setup; Koen to review
- **agent-programme-manager PR #13** (`feature/new-training-docs`) — Marco's training docs; needs review

### Blocked

- Atlas has no MC board → cross-agent requests TO Atlas cannot auto-trigger
- Atlas is commented out in `check-new-tasks.sh` pending a board ID
- Plan: repurpose Veri's archived board `d0cfa49e-edcb-4a23-832b-c2ae2c99bf67` for Atlas

### Next Steps (for next session)

1. Koen to merge: idea PRs #4 #5, all 5 agent memory/updates PRs
2. Complete Atlas board setup: confirm Veri's board repurposed → tell Atlas board ID → uncomment Atlas in `scripts/check-new-tasks.sh` and push
3. Verify Atlas is referenced correctly in all other agents' AGENTS.md
4. Review engine-dev PR #9 and programme-manager PR #13
5. Bootstrap sessions for Axle, Pixel, Beacon, Marco (still pending)
6. Migrate backlog items from BACKLOG.md into Mission Control

### Key Decisions (established this session)

- **CLAUDE.md = pointer list only**: fires on every `claude` CLI invocation; no framing prose
- **Outputs on memory/updates branch**: not a separate branch — reduces PR overhead
- **Output threshold**: analysis/decision/plan/recommendation/work product = substantive; one-liners/ACKs = exempt
- **Cross-agent requests**: tag renamed from `auto-review`; types: Review, Question, Opinion, Feasibility; depth-1 cycle prevention preserved
- **Atlas MC board**: repurpose Veri's existing board rather than create new

### Key Technical Facts

- Atlas workspace: `/home/node/workspace/agents/agent-operations-manager/`
- Org root from this workspace: `../../` (= `/home/node/workspace/`)
- Telegram group: `-5105695997`
- MC API: `http://172.18.0.1:8000`; auth token: `.env` → `AUTH_TOKEN`
- GitHub token: `.env` → `GITHUB_TOKEN`
- Board IDs: Axle `6bddb9d2`, Pixel `ac508766`, Beacon `7cc2a1cf`, Marco `3f1be9c8`, Veri (archived, repurpose for Atlas) `d0cfa49e`
- Agent IDs: Atlas = `operations-manager`; others = `lead-<uuid>`
- Memory commit workflow: commit to `memory/updates` branch → push → long-lived PR → Koen merges
- Always check `git log origin/main..memory/updates --oneline` + open PRs before pushing

## Rule Added — 2026-03-26

**agent-researcher is off-limits.** The folder is being moved to `/home/pi/obsolete/agent-researcher/` on the host (Koen to action). Do not touch, read, edit, or reference any files in `agents/agent-researcher/` — it no longer exists in the active workspace. The GitHub repo is archived.

## Fact Added — 2026-03-26

**SSH username on the Pi is `pi`**, not `koen`. Use `pi@openclaw-pi.tail2d60.ts.net`.
