# Atlas Memory — 2026-03-27

## Session Context (pre-compaction flush)

This file captures state from the compaction summary at 2026-03-27 18:00 UTC.

---

## Open PRs Awaiting Merge (as of 2026-03-27)

| Repo | PR | Branch | Content |
|------|----|--------|---------|
| idea | #4 | `docs/outputs-policy` | Outputs policy design doc section |
| idea | #5 | `docs/cross-agent-requests` | Cross-agent rename |
| idea | #6 | `chore/remove-tmux-vscode` | VSCode tmux removal + Tabby setup + start-agents.sh |
| idea | #7 | `feat/idea-openclaw-setup` | IDEA openclaw config + setup.sh + design doc reframe |
| agent-engine-dev | #13 | `memory/updates` | Outputs section in AGENTS.md |
| agent-engine-dev | #14 | `chore/remove-tmux-vscode` | Strip tmux from .vscode/settings.json |
| agent-console-dev | #9 | `memory/updates` | Outputs section in AGENTS.md |
| agent-site-dev | #9 | `memory/updates` | Outputs section in AGENTS.md |
| agent-programme-manager | #14 | `memory/updates` | Outputs section in AGENTS.md |
| agent-operations-manager | #4 | `memory/updates` | Outputs section + first output file |

Note: agent-engine-dev PR #9 (`test/pr1-disk-simulation`) pre-existing — Koen to review.

---

## Next Steps

1. **Koen:** merge idea PRs #4 #5 #6 #7 and all 5 agent `memory/updates` PRs, agent-engine-dev PR #14
2. **Resolve workflow question:** confirm Telegram chat channel (persistent memory) vs enriched CLAUDE.md (direct Claude debug session) for system bring-up
3. **Atlas:** design unified `idea/platform/compose.yaml` (OpenClaw + MC), write `apply-config.sh`, rename `idea/openclaw/` → `idea/platform/`, update design doc — PR #8
4. **Koen:** execute migration runbook from SSH (stop standalone MC, apply new compose, restart) — after PR #8 merged
5. **Atlas:** verify Atlas board setup (board `d0cfa49e` repurposed from Veri)
6. **Atlas:** review programme-manager PR #13 (`feature/new-training-docs`) — Marco's training docs
7. **Atlas:** bootstrap sessions for Axle, Pixel, Beacon, Marco (still pending)
8. **Atlas:** migrate existing BACKLOG.md items into Mission Control

---

## Key Decisions (locked)

- **CLAUDE.md = pointer list only** — no prose, fires on every `claude` CLI invocation
- **Outputs on `memory/updates` branch** — not a separate branch; reduces PR overhead
- **Cross-agent tag:** `auto-review` → `cross-agent`; types: Review, Question, Opinion, Feasibility; depth-1 cycle prevention preserved
- **Atlas board:** repurposed Veri's board `d0cfa49e-edcb-4a23-832b-c2ae2c99bf67`
- **VSCode tmux integration dropped:** Claude sessions started from SSH remote terminal; tmux for SSH session persistence only
- **Tabby as recommended terminal:** cross-platform, open source; 5 SSH profiles with `tmux new-session -A` per agent; `start-agents.sh` for post-reboot
- **OpenClaw config in `idea` repo:** IDEA setup script + config in `idea/openclaw/` → `idea/platform/`; `app-openclaw` stays generic/MC-free
- **App Disk model dropped for IDEA dev:** school apps use Engine+docking; IDEA dev Pi installs via `setup.sh` directly
- **MC merged into unified compose:** MC moves from standalone to service alongside `openclaw-gateway` in `idea/platform/compose.yaml`; MC API URL → `http://mission-control:8000` after migration
- **IDEA Platform branding:** OpenClaw + MC together = "the IDEA Platform"; `app-openclaw` is upstream dependency
- **Apply step = Koen via SSH:** Atlas designs runbook; Koen executes Docker restart commands to avoid Atlas going offline
- **System bring-up workflow:** under evaluation — Telegram channel vs direct Claude session with enriched CLAUDE.md

---

## Infrastructure Facts

- Atlas workspace: `/home/node/workspace/agents/agent-operations-manager/`
- Org root (from workspace): `../../` = `/home/pi/idea/`
- MC API current: `http://172.18.0.1:8000` → will become `http://mission-control:8000` after unified compose
- Current installation: `/home/pi/openclaw/` — not yet migrated (intentionally kept alive)
- MC currently: standalone stack at `/home/pi/openclaw/mission-control` (submodule)
- SSH: `pi@openclaw-pi.tail2d60.ts.net`
- Telegram group IDs: Axle `-5146184666`, Pixel `-5187034968`, Beacon `-5139661372`, Marco `-5141459717`, Atlas `-5105695997`; CEO `8320646468`
- tmux session names: `claude-operations`, `claude-engine`, `claude-console`, `claude-site`, `claude-programme`
- GitHub: all repos under `koenswings/`; token in `.env` as `GITHUB_TOKEN`
- Agent board IDs: Axle `6bddb9d2`, Pixel `ac508766`, Beacon `7cc2a1cf`, Marco `3f1be9c8`, Atlas `d0cfa49e`

---

## Completed Since Last Memory File

- VSCode tmux terminal profiles removed from `agent-engine-dev/.vscode/settings.json` and `agent-researcher/.vscode/settings.json`
- `agent-terminal-design.md` marked as Superseded (at `agents/agent-operations-manager/design/tmux-vscode-setup/agent-terminal-design.md`)
- "What Needs to Happen (in order)" section removed from design doc
- Tabby terminal + `scripts/start-agents.sh` documented in design doc
- SSH username corrected to `pi` throughout design doc
- `idea/openclaw/openclaw.json` created — full IDEA agent config template
- `idea/openclaw/.env.template` created — credential placeholders
- `idea/openclaw/README.md` created
- `idea/scripts/setup.sh` created — idempotent 8-step install
- Design doc: `## Installing IDEA` as top-level section; app-openclaw demoted to brief subsection
- Architecture decision made: merge MC into unified compose; rename `idea/openclaw/` → `idea/platform/`; brand as "IDEA Platform"

---

## Post-Migration Session (2026-03-27 ~18:00 UTC)

### Migration Status: COMPLETE ✅
- Unified compose (`idea/platform/compose.yaml`) live on `openclaw-pi`
- All 6 containers running on `idea-net`
- All 5 boards intact; IDEA org intact

### Issue Found and Fixed
- `AUTH_TOKEN` in all 5 agent `.env` files was stale (old per-agent tokens from standalone MC)
- New MC uses `AUTH_MODE=local` with single `MC_LOCAL_AUTH_TOKEN` from `platform/.env`
- Fixed: all 5 `.env` files updated to current token (gitignored — not committed)
- Shell note: token contains `+` and `=` — use Python `urllib` to avoid mangling

### BASE_URL Change
- Old: `http://172.18.0.1:8000` (host bridge IP — still works, port 8000 published)
- New: `http://mission-control-backend:8000` (Docker service name — preferred)
- Updated in: all 5 agent TOOLS.md, mc-api SKILL.md, mc-refresh.sh

### MC Frontend URL
- Frontend: `http://localhost:4000` (external) / `https://openclaw-pi.tail2d60.ts.net:4000`
- API: `http://mission-control-backend:8000` (internal) / `http://172.18.0.1:8000` (host bridge)

### PRs Created in This Session
- idea PR #9 (`chore/post-migration-updates`): design doc + mc-api BASE_URL + compose.yaml fixes

### Output File
- `outputs/2026-03-27-1805-platform-migration.md` — full migration log

### MEMORY.md Note
- Infrastructure Facts section in MEMORY.md has stale MC URL — update when MEMORY.md is next edited

---

## MC Board Audit Session (2026-03-27 ~20:00 UTC)

### MC API Token Situation (RESOLVED)
- Per-agent tokens were lost when .env files were updated to shared MC_LOCAL_AUTH_TOKEN
- Restored per-agent tokens to all 5 .env files (Axle, Pixel, Beacon, Marco had original tokens; Atlas needed new one)
- Atlas: new token generated using MC's PBKDF2-SHA256 algorithm; DB updated (agent_token_hash for Quality Manager board)
- Each agent now has its own token for /api/v1/agent/boards/{board_id}/tasks
- Shared MC_LOCAL_AUTH_TOKEN still in platform/.env for the container but NOT used as AUTH_TOKEN in agents

### MC Tasks Cleaned Up
- Deleted 4 stale tasks (Axle board: 3 stale; Atlas board: 1 stale/stale reference to archived files)
- Added 1 missing task to Beacon's board: "Confirm framework: Astro or Hugo"
- Kept: "Test setup design review" on Atlas's board (it's Axle's cross-agent review request to Atlas)

### Files Updated
- README.md: port 4000, IDEA Platform framing, platform/ in directory tree
- BACKLOG.md: regenerated (23 tasks, accurate to MC)
- scripts/export-backlog.sh: new script written and tested

### export-backlog.sh
- Works from agent container via docker exec on openclaw-mission-control-db-1
- Run with: bash /home/node/workspace/scripts/export-backlog.sh
- Excludes done/cancelled tasks; groups by board/status

### Pending: PR #9
- idea PR #9 (chore/post-migration-updates): 5 commits; ready to merge
- Includes: design doc cleanup, BACKLOG, README, export-backlog.sh, compose.yaml fixes

---

## Late-Night Session (2026-03-27 ~23:00 UTC)

### Cross-Agent Identification Standard (COMPLETE)
- Mandatory `[From <AgentName>]` title prefix enforced in all 5 agent AGENTS.md
- Description header template: `**From:** / **Type:** / **Date:**` followed by body + depth-1 warning
- Updated in: design doc (`virtual-company-design.md`), `check-new-tasks.sh`
- `check-new-tasks.sh`: also updated `auto-review` → `cross-agent` throughout, `veri` → `atlas`
- PR #9 amended to include this change (6th commit added); now 6 commits total

### MC Board vs Design Doc Backlog Audit (COMPLETE)
- Fetched all 5 agent boards; confirmed 23 tasks total (Axle: 7, Pixel: 2, Beacon: 3, Marco: 10, Atlas: 1)
- All items match design doc backlog ✅ — no orphans, no missing entries
- Key finding: "Test setup design review" is on Atlas's board in MC (not Axle's) — design doc had wrong attribution
- Decision: keep HQ/Setup and Platform sections in design doc (org-level items not in MC); remove agent subsections (Engine, Console UI, Website, Programme Manager); add pointer to BACKLOG.md

### Design Doc Edit (IN PROGRESS)
- `## Current Backlog` section at line 1200; agent subsections at lines 1212–1231
- Plan: remove agent-specific subsections, keep HQ/Setup + Platform, add `BACKLOG.md` pointer, fix "Test setup design review" attribution
- NOT YET COMMITTED — was in progress at compaction

### MEMORY.md Updated
- Infrastructure Facts section corrected: MC URL → `http://mission-control-backend:8000`, per-agent tokens restored, MC auth model clarified
- MEMORY.md pushed to `memory/updates` branch (PR #4 accumulating)

### Output Files Written This Session
- `outputs/2026-03-27-1805-platform-migration.md` ✅
- `outputs/2026-03-27-2000-mc-board-audit.md` ✅

### PR #9 Final State (6 commits, ready to merge)
1. BASE_URL + compose.yaml production fixes
2. Design doc: SQLite→PostgreSQL, MC ports, "What Needs to Happen" removal, backlog cleanup
3. README.md: port 4000, IDEA Platform framing, platform/ in tree
4. BACKLOG.md + export-backlog.sh
5. Cross-agent identification standard (all 5 AGENTS.md + design doc + check-new-tasks.sh)
6. MEMORY.md infrastructure facts corrected

### Remaining Next Steps
1. Complete design doc edit (remove agent backlog subsections, add BACKLOG.md pointer, fix attribution)
2. Recommend BACKLOG.md sync cadence to Koen
3. Koen merge: idea PRs #4 #5 #6 #7 #9; agent memory/updates PRs (engine #13, console #9, site #9, programme #14, operations #4); engine #14
4. Review engine-dev PR #9 (`test/pr1-disk-simulation`) — Atlas has a task for this
5. Review programme-manager `feature/tapiwa-misheck-briefing` and PR #13 (`feature/new-training-docs`)
6. Bootstrap sessions for Axle, Pixel, Beacon, Marco
7. Migrate HQ backlog items into Mission Control
8. Decide fate of `app-openclaw` repo + GitHub org name
