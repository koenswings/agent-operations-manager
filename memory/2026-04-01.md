# Atlas Memory — 2026-04-01

## Session State (compaction flush)

### Open PRs (awaiting Koen)
- **idea PR #12** — `design/tailscale-remote-management` — tailscale design + scripts: https://github.com/koenswings/idea/pull/12
- **idea PR #18** — `design/openclaw-native-migration` — includes Claude Code executor section: https://github.com/koenswings/idea/pull/18
- **memory/updates PRs pending merge**:
  - Atlas #10: https://github.com/koenswings/agent-operations-manager/pull/10
  - Axle #27: https://github.com/koenswings/agent-engine-dev/pull/27
  - Pixel #17: https://github.com/koenswings/agent-console-dev/pull/17
  - Beacon #12: https://github.com/koenswings/agent-site-dev/pull/12
  - Marco #19: https://github.com/koenswings/agent-programme-manager/pull/19

### Pending Decisions (Koen to confirm)
- **Memory architecture simplification**: stop tracking memory files in main repo; write locally (active immediately) + backup to separate repo. Awaiting Koen confirmation → then update AGENTS.md + PROCESS.md
- **git pull safety fix**: update all 5 AGENTS.md step 0 from `git pull origin main` to `git fetch origin main && git merge --ff-only origin/main || echo "Skipping pull..."`. Awaiting Koen confirmation.
- **Console UI integration**: bundle at build time vs. separate service — Koen decides before Atlas creates tasks for Pixel + Axle
- **Kit setup**: Telegram group, MC board, `agent-app-dev` repo — pending Koen
- **USB stick preparation**: needs Tapiwa's Starlink SSID/password + reusable ephemeral Tailscale auth key
- **Test Pi**: needed for Atlas to run `validate-tailscale-upgrade.sh`

### After memory/updates PR merges
1. Atlas runs `git pull origin main` in all 5 agent repos
2. Koen sends `/init` to Axle, Pixel, Beacon, Marco groups

### Blocked
- Bootstrap sessions for Axle, Pixel, Beacon, Marco
- MC backlog migration
- Marco PRs to review: #13 `feature/new-training-docs`, `feature/tapiwa-misheck-briefing`
- Kit onboarding (full setup pending Koen)
- `proposals/app-dev-agent` branch: has conflict in `design/virtual-company-design.md`; needs resolve + Kit added to design doc

### Recent Merged Work
- idea PR #16: infrastructure discipline rules
- idea PR #19: MC cross-board auth fix (MC_PLATFORM_TOKEN in all 5 agent .env files)
- idea PR #20: Koen-as-hub cross-agent comms
- agent-engine-dev PR #26: Engine HTTP server (serves Console dist/ on port 80; GET /api/store-url)
- All 5 AGENTS.md updated with: PR link reporting rule, memory/updates PR verification, cross-agent comms (Koen-as-hub), git pull step 0, Atlas ops-commit pattern note
- mc-api SKILL.md: cross-board write section + Python example added

### Key Reminders
- Cross-agent comms: `📨 **For [Agent]:** [message]` in own group → Koen forwards
- PR reviews still as GitHub PR comments (not Telegram relay)
- check-new-tasks.sh: disabled with `exit 0` (takes effect after PR #20 merge + git pull)
- No raw markdown/ASCII tables in Telegram — use PNG via ImageMagick or plain bullets
- render_table.py at `/home/node/workspace/skills/telegram-table/scripts/render_table.py`
- Never touch agents/agent-researcher/ (archived, moved to /home/pi/obsolete/)
- MC status gate: can only set task `done` when status is already `review`
- Cross-board writes: MC_PLATFORM_TOKEN + POST /api/v1/boards/{board_id}/tasks (NOT /api/v1/agent/boards/...)
- git worktree procedure in TOOLS.md — never stash+checkout in another agent's repo
