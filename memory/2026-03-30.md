# Atlas Session Memory — 2026-03-30

## Tailscale Upgrade — Status

- PR #12 (`design/tailscale-remote-management`) is **open**, awaiting Koen review/merge
- All scripts committed at `f1c8ee4` on branch `design/tailscale-remote-management`; worktree was `/tmp/idea-tailscale`
- Scripts: `install.sh`, `tailscale-debug-activate.sh`, `tailscaled.service`, `.gitignore`, `validate-tailscale-upgrade.sh`, `prepare-usb-stick.sh`
- Design doc updated: persistent wlan0, SSH-based activation, 3 internet options, automated validation
- **Blocked on**: Tapiwa's Starlink SSID/password (Koen to provide privately) + reusable ephemeral Tailscale auth key for USB package
- **Blocked on**: test Pi provisioned on LAN or Tailscale for Atlas to run `validate-tailscale-upgrade.sh`

## Infrastructure Discipline Rules

- Added to PROCESS.md (PR #16, merged 2026-03-27 / recent session)
- No direct Pi filesystem writes; merge-first; agent repo scope
- PR link reporting rule in PROCESS.md + all 5 agents' AGENTS.md
- memory/updates PR verification rule in all 5 agents' AGENTS.md

## Open memory/updates PRs

| Agent | PR |
|-------|-----|
| Atlas | [#9](https://github.com/koenswings/agent-operations-manager/pull/9) |
| Axle  | [#18](https://github.com/koenswings/agent-engine-dev/pull/18) |
| Pixel | [#11](https://github.com/koenswings/agent-console-dev/pull/11) |
| Beacon| [#11](https://github.com/koenswings/agent-site-dev/pull/11) |
| Marco | [#19](https://github.com/koenswings/agent-programme-manager/pull/19) |

All awaiting Koen merge.

## Other Open PRs

- idea **PR #12** — tailscale design + scripts (open, awaiting merge)
- idea **PR #17** — `fix/run-tests-sandbox-ownership` (open, awaiting merge)
- idea PR #16 — infrastructure discipline — **merged**

## Standup Caveat (2026-03-30)

- `standup-seed.sh` ran 07:30 CET but idea repo was on `fix/run-tests-sandbox-ownership` branch
- Standup commit went to wrong branch; cherry-picked to main
- **Key lesson**: always run `git branch --show-current` on idea repo before committing — may be left on a feature branch

## run-tests.sh

- PR #13 merged; `scripts/run-tests.sh` confirmed on main
- `authorized_keys` on Pi updated with `command=`/`from=172.20.0.0/24`/`restrict`
- `TEST_SSH_KEY` in `.env`; 5/5 tests passing on real Pi (wizardly-hugle)

## Kit 🎒 App Developer

- Sixth operational agent; repo `agent-app-dev`; app repos as git submodules
- Proposal PR #10 was closed; Kit setup (Telegram group, MC board, repo) pending Koen
- Conflict in `design/virtual-company-design.md` on `proposals/app-dev-agent` branch needs resolving + Kit to be added to design doc

## Next Session Priorities

1. Merge PR #12 (tailscale) + PR #17 (run-tests fix) — remind Koen if needed
2. Merge all memory/updates PRs (5 agents)
3. Kit setup: once Koen creates group/board/repo, bootstrap Kit agent
4. Bootstrap sessions for Axle, Pixel, Beacon, Marco still pending
5. Review Marco's PRs (#13 `feature/new-training-docs`, `feature/tapiwa-misheck-briefing`)
6. MC backlog migration still deferred

## Communication Preferences (reminder)

- No raw markdown or ASCII tables in Telegram — use PNG via ImageMagick or plain bullets
- `render_table.py` at `/home/node/workspace/skills/telegram-table/scripts/render_table.py`

## MC Cross-Board Auth Fix (2026-03-31)

- **Root cause**: Agent tokens (`AUTH_TOKEN`) are board-scoped — 403 on all cross-board writes, all agents
- **Fix**: `MC_PLATFORM_TOKEN` (= `MC_LOCAL_AUTH_TOKEN` from `platform/.env`) added to all 5 agent `.env` files
- **Route**: `POST /api/v1/boards/{board_id}/tasks` (admin route, not `/api/v1/agent/boards/...`)
- **Skill updated**: `skills/mc-api/SKILL.md` — new cross-board write section with Python example
- **PR**: idea [#19](https://github.com/koenswings/idea/pull/19) — `fix/mc-cross-board-auth` (open, awaiting merge)
- Verified end-to-end: test task created+deleted on Axle's board using Pixel context
